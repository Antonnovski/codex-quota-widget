# 参与开发

欢迎提交问题和改进建议。

1. 从 `main` 创建功能分支。
2. 使用 .NET 8 在 Windows 上构建并运行项目。
3. 提交前执行 `dotnet build -c Release`。
4. Pull Request 请说明变更目的、界面影响和验证方式。

界面改动请同时检查 100%、125% 和 150% 的 Windows 显示缩放，避免文字截断或卡片换行。

